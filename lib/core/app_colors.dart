import 'package:flutter/material.dart';

class AppColors {
  // Fondos y Superficies (Diseño Claro Premium)
  static const Color backgroundLight = Color(0xFFF8FAFC); // Gris ultra claro de fondo
  static const Color surfaceWhite = Color(0xFFFFFFFF);     // Blanco puro para las tarjetas
  static const Color borderLight = Color(0xFFE2E8F0);      // Líneas divisorias sutiles

  // Identidad de Marca (Verde Esmeralda del Mockup)
  static const Color primaryGreen = Color(0xFF00B074);    // El verde brillante de tus botones
  static const Color primaryGreenPastel = Color(0xFFE6F7F1); // Fondos suaves de éxito

  // Tipografías
  static const Color textDark = Color(0xFF0F172A);         // Títulos oscuros
  static const Color textMuted = Color(0xFF64748B);        // Descripciones y fechas en gris

  // Prioridades (Chips del mockup)
  static const Color priorityHigh = Color(0xFFFFEAEB);     // Fondo Alta
  static const Color textHigh = Color(0xFFFF4D4F);         // Texto Alta
  
  static const Color priorityMedium = Color(0xFFFFF3E6);   // Fondo Media
  static const Color textMedium = Color(0xFFFF9922);       // Texto Media
  
  static const Color priorityLow = Color(0xFFE6F4FF);      // Fondo Baja
  static const Color textLow = Color(0xFF1890FF);          // Texto Baja
}